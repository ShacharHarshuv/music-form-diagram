import { createSection } from "@/app/actions/create-section";
import { deleteSelected } from "@/app/actions/delete";
import { addBars } from "@/app/actions/add-bars";

export const actions = [createSection, deleteSelected, addBars];
